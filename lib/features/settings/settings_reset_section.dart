/// User-facing application data groups that can be reset independently.
enum SettingsResetSection { brokers, dashboards, variables, shortcuts, history, connection, publishing, userInterface, updates }
