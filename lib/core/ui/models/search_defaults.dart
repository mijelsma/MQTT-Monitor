/// Selects which topic-row fields participate in monitor filtering.
enum SearchScope { all, topic, value }

/// Selects whether every search term or any search term must match.
enum SearchMatchMode { any, all }
